# Steps 12-18 実装: 打席取消・WP/PB/BK・個人成績・バリデーション強化

## 実装順序
1. Step 18: サーバーサイドバリデーション強化（Zod）
2. Step 12: 打席の取り消し（Undo）
3. Step 13: WP/PB/BK記録
4. Step 14: 個人成績ページ

## 完了チェックリスト

### Step 18: サーバーサイドバリデーション
- [x] `app/(main)/games/validation.ts` に Zod スキーマ定義を作成
- [x] `recordAtBatAction` にスキーマバリデーション + game status チェック追加
- [x] `startGameAction` に lineup 存在チェック + status チェック追加
- [x] `saveLineupAction` に game status チェック追加
- [x] `changePitcherAction` にバリデーション追加
- [x] `finishGameAction` に status チェック追加
- [x] `recordStealAction` にバリデーション追加
- [x] `substitutePlayerAction` にバリデーション追加
- [x] `changePositionAction` にバリデーション追加
- [x] `updateGameAction` にバリデーション追加
- [x] 既存テスト67件すべてパス

### Step 12: 打席取消（Undo）
- [x] `undoLastAtBatAction` を `actions.ts` に追加（投手成績逆算込み）
- [x] 入力画面ヘッダーに「戻す」ボタン + AlertDialog 追加
- [x] `undoLastAtBatAction` テスト5件追加

### Step 13: WP/PB/BK
- [x] マイグレーション `2602230300_add_runner_event_types.sql` 作成
- [x] `recordRunnerAdvanceAction` を `actions.ts` に追加
- [x] `useGameState.ts`: `applyStealEvents` → `applyRunnerEvents` にリネーム・拡張
- [x] 入力画面に「WP/PB/BK」ボタン + RunnerAdvanceDialog 追加
- [x] `recordRunnerAdvanceAction` テスト6件追加
- [x] `lib/supabase/types.ts` 再生成（マイグレーション適用後）

### Step 14: 個人成績ページ
- [x] `app/(main)/team/[id]/players/[playerId]/page.tsx` 作成
- [x] `BatterStatsTable` に `mode: "player"` 追加（試合日・対戦相手付き）
- [x] `PitcherStatsTable` に `mode: "player"` 追加
- [x] 選手一覧ページ: 各選手名を個人成績ページへのリンクに変更
- [x] 通算成績ページ: 選手名を個人成績ページへのリンクに変更

### 全体
- [x] `pnpm lint` パス
- [x] `pnpm build` パス（新ルート `/team/[id]/players/[playerId]` 確認）
- [x] `pnpm test` パス（158テスト / 11ファイル）
