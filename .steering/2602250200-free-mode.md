# フリーモード機能の実装

## 概要

チーム未所属でも試合を記録できる「フリーモード」機能を追加する。任意の2チーム名を入力して試合を記録でき、結果は通算成績に含めない。

## 完了要件チェックリスト

### DB・マイグレーション
- [ ] `games` テーブルにカラム追加: `created_by`, `is_free_mode`, `home_team_name`, `visitor_team_name`
- [ ] `team_id` を NULLable 化
- [ ] `use_dh` カラムが存在確認（既存: 2602220100 で追加済み）
- [ ] CHECK 制約: チーム試合 → team_id NOT NULL / フリー → team_id NULL + 両チーム名 NOT NULL
- [ ] 既存行の `created_by` を `teams.owner_id` からバックフィル
- [ ] RLS ポリシー更新: `games_all` を置き換え（チームOR / フリーモード作成者）
- [ ] 子テーブルのRLSポリシー更新（`games` の条件変更に追従）
- [ ] 通算成績ビュー（`v_batter_career_stats`, `v_pitcher_career_stats`）に `WHERE g.is_free_mode = false` を追加
- [ ] `pnpm supabase:reset` で適用確認

### 型再生成
- [ ] `pnpm supabase:gen-types` 実行

### バリデーション
- [ ] `createFreeGameSchema` を `validation.ts` に追加
- [ ] `updateFreeGameSchema` を `validation.ts` に追加

### Server Actions
- [ ] `createFreeGameAction` を `actions.ts` に追加（`team_id: null, is_free_mode: true, created_by: user.id`）
- [ ] `updateGameAction` をフリーモード対応（`home_team_name` / `visitor_team_name` 更新）

### 試合作成ページ (`games/new/page.tsx`)
- [ ] モード切替トグル（「チーム試合」/「フリーモード」）
- [ ] フリーモード時: チーム選択非表示、ホーム/ビジター両チーム名入力
- [ ] フリーモード時: ホーム/ビジタートグル非表示（両チーム名入力で決定）
- [ ] チーム未所属でもフリーモードなら試合作成可能

### useGameState フック (`hooks/useGameState.ts`)
- [ ] `GameInfo` に `is_free_mode`, `home_team_name`, `visitor_team_name`, `created_by` 追加
- [ ] `team_id` を `string | null` に変更
- [ ] select句にカラム追加
- [ ] フリーモード時は teams テーブルクエリをスキップ
- [ ] `myTeamName` を `home_team_name` にセット

### 試合一覧 (`page.tsx`)
- [ ] フリーモード試合を `created_by` + `is_free_mode` で追加取得
- [ ] チーム試合とマージして `game_date` 降順ソート
- [ ] select句に `is_free_mode, home_team_name, visitor_team_name` 追加
- [ ] 「新規試合登録」ボタンを常に表示

### GameCard (`components/game/GameCard.tsx`)
- [ ] props に `is_free_mode?`, `home_team_name?`, `visitor_team_name?` 追加
- [ ] フリーモード時: `{home} vs {visitor}` + 「フリー」バッジ
- [ ] フリーモード時のスコア表示: `{home得点} - {visitor得点}`

### 試合詳細 (`games/[id]/page.tsx`)
- [ ] select句にフリーモード関連カラム追加
- [ ] フリーモード時: teams クエリをスキップ
- [ ] ヘッダー: フリーモード時は `{home_team_name} vs {visitor_team_name}`

### ラインナップ (`games/[id]/lineup/page.tsx`)
- [ ] フリーモード時: players 取得をスキップ
- [ ] 両チームともテキスト入力モード（`allowUnregistered` 相当）
- [ ] チーム名ラベルに `home_team_name` / `visitor_team_name` 使用

### 入力ページ (`games/[id]/input/page.tsx`)
- [ ] useGameState 更新で大部分は自動対応
- [ ] 選手交代ダイアログ: フリーモード時はベンチ選手リスト取得スキップ、テキスト入力のみ

### 試合編集 (`games/[id]/edit/page.tsx`)
- [ ] フリーモード時: `home_team_name` / `visitor_team_name` 入力欄表示
- [ ] ホーム/ビジタートグル非表示

### seed.sql
- [ ] フリーモードのテスト試合を追加

### テスト
- [ ] `createFreeGameAction` のユニットテスト
- [ ] バリデーションスキーマのテスト
- [ ] GameCard フリーモード表示テスト
- [ ] `pnpm test` で既存テスト通過確認

### 最終検証
- [ ] `pnpm build` 成功
- [ ] `pnpm lint` パス

## 修正対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `supabase/migrations/2602250200_add_free_mode.sql` | 新規: マイグレーション |
| `lib/supabase/types.ts` | 自動再生成 |
| `app/(main)/games/validation.ts` | スキーマ追加 |
| `app/(main)/games/actions.ts` | Action追加・更新 |
| `app/(main)/games/new/page.tsx` | モード切替UI |
| `hooks/useGameState.ts` | GameInfo拡張、フリーモード対応 |
| `app/(main)/page.tsx` | フリーモード試合の取得・表示 |
| `components/game/GameCard.tsx` | フリーモード表示 |
| `app/(main)/games/[id]/page.tsx` | 詳細ページ対応 |
| `app/(main)/games/[id]/lineup/page.tsx` | 両チームテキスト入力 |
| `app/(main)/games/[id]/input/page.tsx` | 交代ダイアログ対応 |
| `app/(main)/games/[id]/edit/page.tsx` | 編集フォーム対応 |
| `supabase/seed.sql` | テストデータ追加 |

## 技術的な注意点

### DB
- `created_by` のバックフィルは `games.team_id → teams.owner_id` を使用
- 子テーブルの RLS は `game_id IN (SELECT id FROM games WHERE ...)` パターンなので、games のポリシー変更が自動的に伝播
- 通算成績ビューは `JOIN players p ON p.id = l.player_id`（INNER JOIN）で未登録選手が自動排除されるが、安全策として `WHERE g.is_free_mode = false` も追加

### フロントエンド
- フリーモード時は `is_home` の概念がない → home/visitor は入力順で決定
- ラインナップは両チームとも手動入力（`players` prop なし）
- 選手交代ダイアログはベンチ選手リスト不要、テキスト入力のみ
