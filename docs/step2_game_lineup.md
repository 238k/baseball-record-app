# 野球試合記録アプリ 実装指示 Step 2
# 試合登録・オーダー入力

## 前提

Step 1が完了し、以下が動作していること：
- 認証（ログイン・ログアウト）
- チーム作成・メンバー管理
- 選手登録・管理
- shadcn/uiがインストール済みであること

---

## このステップの目標

- 試合を登録できる
- 自チーム・相手チームのオーダーを入力できる
- トップ画面に試合一覧が表示される

---

## 追加・変更するファイル

```
src/
├── app/
│   └── (main)/
│       ├── page.tsx                        # 試合一覧を追加
│       └── games/
│           ├── new/page.tsx                # 試合登録
│           └── [id]/
│               ├── page.tsx                # 試合詳細（簡易版）
│               └── lineup/page.tsx         # オーダー登録
├── components/
│   └── game/
│       ├── GameCard.tsx                    # 試合カード
│       └── LineupEditor.tsx                # オーダー入力コンポーネント
```

---

## 実装仕様

### トップ画面 `/`（更新）

所属チームの試合一覧を表示：
- 試合日・相手チーム名・ホーム/ビジター・スコア（終了試合のみ）・ステータス
- ステータスバッジ：`scheduled`=「試合前」、`in_progress`=「試合中」、`finished`=「終了」
- 「新規試合登録」ボタン
- 試合カードをタップすると試合詳細へ

### 試合登録 `/games/new`

入力項目：
- 相手チーム名（必須）
- 試合日（必須・デフォルト今日）
- 場所
- ホーム/ビジター（トグル）
- イニング数（デフォルト7・草野球は7回が多い）

登録後はオーダー登録画面へ遷移。

### オーダー登録 `/games/[id]/lineup`

**自チームのオーダー：**

打順1〜9まで以下を入力：
- 選手：登録選手からセレクト（背番号・名前表示）または「未登録選手」を選んで名前を直接入力
- 守備位置：投・捕・一・二・三・遊・左・中・右・DH からセレクト

**相手チームのオーダー：**

打順1〜9まで名前のみ入力（全員未登録扱い）。
入力省略可能（省略した場合は「相手選手1」などのプレースホルダーで記録）。

**投手の初期設定：**

自チーム・相手チームそれぞれ先発投手を指定（lineupから選択）。
`pitching_records` にレコードを作成（inning_from=1）。

**保存後の動作：**

「記録を開始する」ボタンを表示。タップすると試合ステータスを `in_progress` に変更し、記録入力画面へ遷移。

### 試合詳細 `/games/[id]`（このステップでは簡易版）

- 試合情報（日付・相手・場所）
- オーダー表（自チーム・相手）
- 「記録を入力する」ボタン（`in_progress`のみ表示）
- 「オーダーを編集する」ボタン（`scheduled`のみ表示）

---

## UI・UX要件

- タブレット（iPad）での操作を主に想定
- shadcn/uiのコンポーネントを積極的に使用すること
- 選手セレクトはshadcn/uiの `Select` を使用、背番号順でソート
- 守備位置は日本語略称で表示（投・捕・一・二・三・遊・左・中・右・DH）
- 入力中のデータはページ離脱時に確認ダイアログ（shadcn/uiの `AlertDialog`）を表示
- ボタンは `size="lg"` を基本とすること

---

## 完了条件

- [ ] 試合を登録できる
- [ ] 登録した試合がトップ画面に表示される
- [ ] 自チームのオーダーを登録選手から選んで入力できる
- [ ] 未登録選手の名前を直接入力できる
- [ ] 相手チームの選手名を入力できる
- [ ] 先発投手を指定できる
- [ ] 「記録を開始する」で試合ステータスがin_progressになる
- [ ] 試合詳細でオーダー表が確認できる
